import 'package:flutter/material.dart';

class GainerLooserTab extends StatelessWidget {
  const GainerLooserTab({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
